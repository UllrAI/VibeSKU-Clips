# VibeSKU Clips Design Guidelines

VibeSKU Clips 是面向中文内容创作者、电商运营与小型视频团队的本地优先 AI 视频工作台。设计的首要目标不是展示 AI 能力，而是让用户更快拿到一条可信、可编辑、可发布的视频。

本规范参考 [Vercel `design.md`](https://vercel.com/design.md) 的工作方法，并结合 VibeSKU Clips 的用户、业务流程与桌面端使用场景重新制定。我们吸收的是“先确定用户任务、证据和阅读路径，再决定构图；用排版、网格和密度建立层级；让表面与边界有理由”的方法，不复制 Vercel 的品牌外观。

这不是灵感文档，而是实现契约。设计、代码审查和页面验收都必须能回到本文件中的令牌、页面类型、组件规则与验收矩阵；当实现与本文件冲突时，应修正实现或在同一变更中更新规范。

## 1. 设计原则

### 1.1 先行动，再配置

- 首屏直接暴露核心输入与主操作，不用营销 Hero 阻挡工作流。
- 新手路径只要求完成当前动作必需的信息；高级控制按需展开。
- 配置缺失时说明影响、提供明确出口，并保留用户已经填写的内容。

### 1.2 复杂能力，渐进呈现

- 极简模式提供一条可完成的默认路径；专业模式展示镜头、模型和质量控制。
- 默认值必须能产出合理结果，不让用户先理解模型术语。
- 空态、加载、失败、部分完成和恢复状态都必须提供下一步。

### 1.3 结果可解释、成本可预期

- 涉及生成时间、模型费用、素材来源和发布风险时，提前说明。
- 状态不只依赖颜色，配合标签、图标或文本。
- 错误文案同时说明原因与解决方法，避免只显示技术错误。

### 1.4 品牌有温度，工具要克制

- Landing 可以更有节奏、氛围与品牌表达；进入项目后回归高密度、低干扰的专业工作台。
- 动效用于解释状态、引导注意或提供轻微愉悦，不持续抢占注意力。
- 禁止蓝紫渐变。禁止以紫色、靛蓝或霓虹蓝作为品牌主视觉。

### 1.5 层级来自关系，不来自特效

- 优先用标题层级、内容顺序、对齐、留白和密度表达主次。
- 页面默认是一张连续画布；只有交互、选中、警告或无法仅靠间距表达的真实分组，才使用表面或边界。
- 禁止用大阴影、发光、玻璃模糊、装饰渐变或悬浮抬升制造“高级感”。
- 每个页面只保留一个视觉锚点；次要内容必须主动降噪。

## 2. 视觉语言

### 2.1 品牌关键词

`创作现场`、`可信工具`、`暖色能量`、`本地掌控`、`快速成片`。

视觉隐喻来自剪辑台、时间线、片场标记、暖色工作灯和输出状态，不使用通用 AI 星云、蓝紫光晕或玻璃拟态堆叠。

### 2.2 色彩

VibeSKU Clips 同时提供亮色与暗色工作台，首次访问跟随系统偏好，之后记住用户选择。色彩基线参考相邻产品 `anikuku` 的“纸张 / 墨色 / 土质颜料”体系，并保留 VibeSKU Clips 的视频创作品牌语义。颜色通过 CSS 语义令牌使用，业务组件不得重复硬编码品牌色。

| 角色 | 深色 | 浅色 | 用途 |
| --- | --- | --- | --- |
| Background | `oklch(0.15 0.02 50)` | `oklch(0.99 0.01 85)` | 暖炭黑 / 纸张白页面底色 |
| Surface | `oklch(0.18 0.02 50)` | `oklch(1 0 0)` | 卡片、弹层 |
| Raised | `oklch(0.22 0.02 50)` | `oklch(0.96 0.02 85)` | 悬浮与次级层 |
| Foreground | `oklch(0.95 0.01 85)` | `oklch(0.2 0.02 50)` | 粉笔白 / 墨黑主要文字 |
| Muted | `oklch(0.7 0.02 85)` | `oklch(0.45 0.02 50)` | 次要文字 |
| Border | `oklch(0.25 0.02 50)` | `oklch(0.88 0.02 85)` | 石墨边界与分隔 |
| Primary | `oklch(0.65 0.15 45)` | `oklch(0.55 0.15 45)` | Burnt Sienna 主操作、选中态 |
| Accent | `oklch(0.25 0.02 50)` | `oklch(0.95 0.04 85)` | 暖色标记、少量 Magic UI 点缀 |
| Success | `oklch(0.72 0.13 150)` | `oklch(0.5 0.12 150)` | 成功与就绪 |
| Warning | `oklch(0.78 0.13 80)` | `oklch(0.5 0.13 65)` | 风险与待处理 |
| Destructive | `oklch(0.6 0.18 25)` | `oklch(0.6 0.18 25)` | 删除与不可逆错误 |

约束：

- 品牌渐变仅允许在 Landing 的极小装饰或 Magic UI 边缘光束中使用暖橙到琥珀；按钮默认使用纯色。
- 每个页面和组件都必须在亮色、暗色模式下可读；禁止只为其中一种模式写死背景、前景或边框。
- 不使用蓝紫、紫粉或青紫渐变。
- 图表沿用 `anikuku` 的土红、鼠尾草、靛墨、赭黄和暗紫颜料组；不得将其中任意两色组合成蓝紫渐变，状态必须带文本。
- 非中性背景上的文字、边框和阴影要向同一暖色相轻微偏移。

### 2.3 字体

- UI 与正文：Geist Sans；中文按系统字体回退。
- 数字、时长、价格、模型 ID：Geist Mono，并使用等宽数字。
- 页面标题：`24–32 px / 1.2 / 650–700`。
- 区块标题：`16–18 px / 1.35 / 600`。
- 正文：`14 px / 1.6 / 400`；辅助文字不小于 `12 px`。
- Landing 主标题可以达到 `48–64 px`，但移动端从 `36 px` 起。
- 中文界面不使用英文式全大写标签；按钮和标题使用自然的句式表达。

### 2.4 间距、圆角、边界与层级

- 基础间距：`4 px`；常用序列为 `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。
- 控件圆角：`8–10 px`；卡片：`12–16 px`；大容器最多 `20 px`。
- 子元素圆角不得大于父容器，嵌套时保持同心关系。
- 普通表面使用实体背景和 `1 px` 语义边框，不使用投影。浮层也优先使用边框与遮罩；确需与内容分离时，阴影模糊半径不得超过 `16 px`、不带品牌色。
- 可点击表面通过边框色、背景色和焦点环反馈，不位移、不抬升、不发光。
- 不把每个区块包成卡片，不嵌套无意义面板。相关内容先通过标题、分隔线、留白和共享对齐分组。
- 内容宽度按任务选择：阅读与单列输入 `720–896 px`，创建/设置工作流 `1120–1280 px`，素材/生产/时间线 `1280–1500 px`。正文行宽保持约 `60–68` 个字符，不能因为画布变宽而无限拉长。

### 2.5 页面网格

- 桌面内容区使用 12 列思路组织，平板 6 列，移动端 4 列；不要求暴露网格线，但所有主要边缘必须共享对齐关系。
- 页面外边距：移动端 `16 px`，平板 `24 px`，桌面 `32 px`；超宽屏只增加画布两侧留白，不放大控件。
- 主任务与辅助信息并列时，优先 `8:4` 或 `9:3`，辅助栏不得挤压主任务到不可读宽度；窄屏必须回到单列。
- 表单标签、输入、帮助与错误属于一个字段组；步骤标题与步骤内容属于一个区块，不用额外卡片重复包裹。
- 一屏内重复出现 3 个以上同权重边框容器，必须重新判断是否可以通过分隔、表格、列表或分组标题降噪。

## 3. 页面类型

### 3.1 Landing / 工作台入口

- 目标：让首次用户在 10 秒内理解价值并开始上传、粘贴链接或输入主题。
- 使用实体暖橙和低对比点阵建立识别度，不使用环境光晕、文字发光、呼吸阴影或围绕整张表单的边缘光束。
- 宽屏首屏采用 `主任务区 + 320–360 px 生成摘要栏`，两者是同级区域；不得把来源、字段、生成方式和高级设置继续嵌套进一张窄卡。
- 示例紧邻其服务的输入；最近项目和热点位于主任务之后，不与创建动作竞争。
- 免费与付费生成方式必须显示费用归属、预计耗时和产出差异。
- Landing 可以有独立视觉节奏，但颜色、字体、控件和交互状态仍服从本规范。

### 3.2 列表与资产页

- 标题、说明、主操作和筛选形成稳定页头。
- 卡片优先展示名称、状态、更新时间和下一动作，避免大片无信息留白。
- 空态包含解释、主要行动和可选示例；删除提供确认或可撤销窗口。

### 3.3 创建与配置页

- 按真实流程分段，步骤标题说明当前要完成的任务。
- 必填项带文字或符号并关联可访问标签；不预先禁用提交来隐藏验证反馈。
- 高级选项使用折叠区，不与默认路径争夺注意力。
- API Key 等敏感输入不进入日志、URL 或持久化设计文档。

### 3.4 项目生产页

- 页面级导航与项目步骤保持一致位置；当前阶段、完成阶段和异常阶段同时有图形与文本。
- 编辑区、预览区和检查区按任务关系布局，不机械套用相同卡片。
- 生成、分析和导出进度使用可中断或可恢复状态；异步更新通过 `aria-live="polite"` 宣布。

### 3.5 设置页

- 默认先展示推荐路径，再展示分平台高级配置。
- 平台卡片只在展开或选中时显示完整字段，降低首屏噪声。
- 每个设置说明即时保存、作用范围和测试状态。

## 4. 组件规范

### 4.1 基础组件

- 交互控件优先使用 `components/ui` 中的 shadcn/ui 组件；业务页面不重新实现 Button、Input、Select、Dialog、Tabs、Tooltip 等基础行为。
- 图标统一使用 Lucide，默认 `16 px`、`1.75–2 px` 描边；禁止手写近似 SVG 或 emoji 代替界面图标。
- `Button`：桌面高度至少 `36 px`，移动端主要操作至少 `44 px`；加载时保留原标签并显示进度。
- `Input`：移动端字号至少 `16 px`；占位符给出示例并以省略号 `…` 结尾。
- `Card`：默认仅有实体表面和细边框；只有真实分组需要它。可点击卡片 hover 只改变边框与背景。
- `Tabs`：用于同级内容切换，不代替跨页面导航；可分享状态优先进入 URL。
- `Dialog`：正确陷阱焦点，关闭后归还焦点；破坏性操作写清对象和后果。

### 4.2 Magic UI

- 只用于 Landing、首次连接成功、关键生成入口等高价值时刻。
- 推荐：`DotPattern` 作为 Landing 的低对比背景；`BorderBeam` 仅限非表单型、短暂成功提示，不围绕持续交互的主任务容器。
- `AnimatedShinyText` 与 `ShimmerButton` 默认禁用；它们会降低文字稳定性或把装饰误认为操作状态，只有经过单独可用性验证后才可启用。
- 禁止在设置、表格、长表单和每张卡片上重复动画。
- 所有动画支持 `prefers-reduced-motion`，不使用 `transition: all`。

### 4.3 状态优先级

- 主操作：每个任务区最多一个实心品牌按钮；同屏其余动作使用次级、描边或文字按钮。
- 选中：`border-primary + 低对比 primary 背景 + 文本/图标`，不使用阴影或发光。
- 聚焦：使用统一 `3 px` 焦点环；焦点环是无障碍反馈，不计入装饰阴影。
- 成功、警告、错误：必须同时包含图标或文字标签，不只换颜色。
- 禁用：保留原标签和结构，说明无法操作的原因；不得用不可见或无提示的禁用隐藏校验。

## 5. 交互与状态

- 全流程可通过键盘完成，焦点环清晰且不被粘性区域遮挡。
- 桌面点击目标至少 `24 px`；移动端至少 `44 px`。
- 导航使用 `<Link>` / `<a>`，动作使用 `<button>`。
- 拖拽上传必须同时提供点击选择与键盘入口。
- 保存、生成、连接等异步状态使用“正在保存…”等自然文案，并避免短暂闪烁。
- 用户可安全预期成功时使用乐观更新；失败时回滚并提供重试或撤销。
- 可分享的标签、筛选、分页和展开状态优先写入 URL。
- 支持系统缩放，不禁用粘贴，不在输入过程中阻止用户键入。

## 6. 内容与本地化

- 默认中文文案直接、具体、行动导向；英文保持相同信息层级，不逐字直译。
- 一个概念只使用一个名称。例如统一使用“项目”“成片”“API Key”。
- 按钮说明结果，如“开始生成脚本”“保存 API Key”，避免“继续”“确认”。
- 使用数字表示数量，数字与单位之间留不换行空格，例如 `30 秒`、`10 MB`。
- 使用中文省略号 `…`，使用弯引号；标题避免孤行与尴尬断句。
- 日期、金额、时长和数字按当前语言环境格式化。

## 7. 可访问性与性能

- 页面拥有唯一、准确的标题和一个清晰的 `<h1>`；主内容提供跳转链接。
- 优先语义 HTML，再使用 ARIA；图标按钮必须有可描述的 `aria-label`。
- 装饰图案使用 `aria-hidden`，图片设置准确 `alt`，视频提供字幕或文本说明。
- 不仅依赖颜色表达状态；文字与背景维持可读对比。
- 所有动效遵循 `prefers-reduced-motion`；只动画 `opacity` 和 `transform` 等合成属性。
- 禁止 `transition: all`。
- 图片与视频预留明确尺寸；首屏必要资源优先加载，其余延迟加载。
- 大列表使用虚拟化或 `content-visibility`；昂贵媒体处理离开主线程。

## 8. 页面验收清单

每次设计或 UI 变更至少检查：

1. Landing：主动作在首屏可见，品牌风格独立且没有蓝紫渐变。
2. 项目列表、商品库、主播库：加载、空、少量、密集和删除状态完整。
3. 新建项目、热点成片、爆款复刻、批量任务：默认路径清晰，高级项按需出现。
4. 脚本、素材、生产、字幕、视频、导出：项目阶段与下一步一致，错误可恢复。
5. 设置：推荐路径、单平台配置、测试失败和敏感字段行为明确。
6. 桌面、移动与超宽布局；键盘导航、焦点、缩放和减少动画偏好。
7. 页面标题、空态文案、错误出口和异步播报。
8. 全仓搜索不得出现蓝紫品牌色、蓝紫渐变或无理由的硬编码品牌色。
9. 全仓搜索不得出现大阴影、品牌色阴影、文字发光、玻璃卡片或 hover 位移。
10. 页面在 `390 / 768 / 1280 / 1440 / 1728 px` 宽度下不存在被截断的主要操作、孤立窄栏或无意义大空白。

## 9. 页面与组件验收矩阵

下面的矩阵定义页面的主要用户任务、默认画布和首要验收点。页面实现可以有独立构图，但不能偏离对应任务。

| 路由/范围 | 用户的首要任务 | 画布 | 必须优先检查 |
| --- | --- | --- | --- |
| `/start` | 选择输入方式并立即开始 | 工作台 `1180–1240 px`；主区自适应 + `340 px` 摘要栏 | 输入与生成设置是同级任务；高级项默认折叠；示例紧邻输入；最近项目与热点降为辅助 |
| `/projects` | 找到并继续一个项目 | 标准 `1280 px` | 搜索与筛选同组；状态、更新时间、下一步清晰；空态可行动 |
| `/products`、`/presenters` | 管理可复用资产 | 标准 `1280 px` | 新增与浏览分层；密集列表不使用重复大卡片；删除后果清楚 |
| `/project/new` | 从商品资料创建带货项目 | 工作流 `1280 px` | 主列完成任务，右栏只解释进度；高级项折叠；底部主操作不与内容脱节 |
| `/project/topic`、`/project/clone` | 从主题或参考视频开始 | 工作流 `1024–1280 px` | 输入、来源可信度、生成方式和费用在提交前明确 |
| `/batch` | 配置并提交多个任务 | 工作流 `1280 px` | 批次、共享设置、单项例外关系清楚；总量与预计成本可见 |
| `/media-lab` | 分析媒体并采取下一动作 | 宽画布 `1280 px` | 上传和结果直接关联；技术信息按需展开；错误可恢复 |
| `/settings` | 完成推荐配置并验证可用 | 标准 `1280 px` | 先推荐路径后平台细节；导航与内容共享基线；保存/测试反馈就地出现 |
| `/project/[id]/script` | 生成、比较并确定脚本 | 宽画布 `1500 px` | 阶段状态、脚本差异、选中结果与下一步明确 |
| `/project/[id]/assets` | 准备和修复镜头素材 | 宽画布 `1500 px` | 镜头与素材关系稳定；批量动作不遮蔽单项状态 |
| `/project/[id]/production` | 检查全流程并处理阻塞 | 宽画布 `1500 px` | 阶段、阻塞、修复动作按影响排序；右侧信息不挤压主区域 |
| `/project/[id]/transcript` | 校对字幕与时间轴 | 宽画布 `1500 px` | 文本、时间与播放头可对应；快捷操作有键盘与文本提示 |
| `/project/[id]/video` | 编辑成片并预览结果 | 宽画布 `1500 px` | 预览、时间线、属性面板形成稳定主次；窄屏按任务顺序堆叠 |
| `/project/[id]/export` | 确认质量并导出/发布 | 工作流 `1280 px` | 质量门槛、平台差异、风险与最终操作在一次阅读路径中完成 |
| `components/ui` | 提供一致、可访问的基础行为 | 全局 | 明暗主题、键盘、焦点、禁用、错误、弹层与减少动画状态 |

每次页面 Review 使用同一顺序：`用户任务 → 首屏答案/主动作 → 信息顺序 → 网格与宽度 → 状态与错误 → 文案 → 键盘/主题/响应式`。不得先从颜色、圆角或动画开始。

## 10. 文案 Review 规则

- 页面标题回答“用户在这里完成什么”，副标题只补充范围或结果，不重复标题。
- 主按钮使用“动词 + 结果”，例如“生成脚本”“保存并测试”；避免“确定”“下一步”“立即体验”等脱离上下文的泛化词。
- 帮助文案只保留会改变选择的信息：费用、耗时、输入要求、影响范围、不可逆后果。
- 空态先说明为什么为空，再给主要行动；错误先说发生了什么，再给恢复动作，不暴露无用的内部术语。
- 中文使用自然句式，不把英文产品营销短语直译为中文，不使用夸张承诺、AI 套话和感叹号堆叠。
- 同一概念全站只用一个名称：项目、商品、主播、脚本、镜头、素材、字幕、成片、导出、API Key。

## 11. 实现约束

- 设计令牌集中维护在 `src/app/globals.css`，组件通过语义变量消费。
- shadcn/ui 组件保留其无障碍行为，只通过变量、variants 与 `className` 做视觉适配。
- 业务组件不得复制基础控件实现，不新增与现有库重叠的依赖。
- 删除废弃样式与旧品牌色，不保留双主题 fallback。
- 禁止新增 `glass-card`、`neon-glow`、`shadow-lg`、`shadow-xl`、`drop-shadow-*`、`text-shadow` 或以品牌色生成的 `box-shadow`。
- 设计改造完成后运行 lint、类型检查/构建、测试，并用浏览器检查关键页面。

## 12. Review 记录要求

- 每次设计变更在 PR/提交说明中列出受影响页面、共享组件与用户任务，不用“全面优化”代替证据。
- 浏览器验收至少保存一组亮色与暗色关键页截图；涉及工作流时记录主路径是否可完成。
- 无法进入的动态页面、需要真实凭据的状态或缺少数据的分支，必须明确标为未验证，不能以静态代码检查冒充视觉验收。
