# ClipForge Design Guidelines

ClipForge 是面向中文内容创作者、电商运营与小型视频团队的本地优先 AI 视频工作台。设计的首要目标不是展示 AI 能力，而是让用户更快拿到一条可信、可编辑、可发布的视频。

本规范参考 [Vercel Web Interface Guidelines](https://vercel.com/design/guidelines) 的交互、可访问性、性能和内容原则，并结合 ClipForge 的用户、业务流程与桌面端使用场景重新制定。Vercel 的品牌偏好不直接移植到 ClipForge。

## 1. 设计原则

### 1.1 先行动，再配置

- 首屏直接暴露核心输入与主操作，不用营销 Hero 阻挡工作流。
- 新手路径只要求完成当前动作必需的信息；高级控制按需展开。
- 配置缺失时说明影响、提供明确出口，并保留用户已经填写的内容。

### 1.2 复杂能力，渐进呈现

- 小白模式提供一条可完成的默认路径；导演模式展示镜头、模型和质量控制。
- 默认值必须能产出合理结果，不让用户先理解模型术语。
- 空态、加载、失败、部分完成和恢复状态都必须提供下一步。

### 1.3 结果可解释、成本可预期

- 涉及生成时间、模型费用、素材来源和发布风险时，提前说明。
- 状态不只依赖颜色，配合标签、图标或文本。
- 错误文案同时说明原因与解决方法，避免只显示技术错误。

### 1.4 品牌有温度，工具要克制

- Landing 可以更有节奏、氛围与品牌表达；进入项目后回归高密度、低干扰的专业工作台。
- 动效用于解释状态、引导注意或提供轻微愉悦，不持续抢占注意力。
- 禁止蓝紫渐变。禁止以紫色、靛蓝或霓虹蓝作为品牌主视觉。

## 2. 视觉语言

### 2.1 品牌关键词

`创作现场`、`可信工具`、`暖色能量`、`本地掌控`、`快速成片`。

视觉隐喻来自剪辑台、时间线、片场标记、暖色工作灯和输出状态，不使用通用 AI 星云、蓝紫光晕或玻璃拟态堆叠。

### 2.2 色彩

ClipForge 同时提供亮色与暗色工作台，首次访问跟随系统偏好，之后记住用户选择。颜色通过 CSS 语义令牌使用，业务组件不得重复硬编码品牌色。

| 角色 | 深色 | 浅色 | 用途 |
| --- | --- | --- | --- |
| Background | `#0D0D0C` | `#F7F6F2` | 页面底色 |
| Surface | `#151513` | `#FFFFFF` | 卡片、弹层 |
| Raised | `#1D1C19` | `#F0EEE8` | 悬浮与次级层 |
| Foreground | `#F5F3EE` | `#1A1917` | 主要文字 |
| Muted | `#A7A39A` | `#6F6B63` | 次要文字 |
| Border | `#2A2925` | `#DEDAD1` | 边界与分隔 |
| Primary | `#F97316` | `#E85D04` | 主操作、选中态 |
| Primary Hover | `#FB923C` | `#C94F03` | 悬停态 |
| Accent | `#F2B84B` | `#B86E00` | 少量强调、Magic UI 点缀 |
| Success | `#34D399` | `#087F5B` | 成功与就绪 |
| Warning | `#FBBF24` | `#9A5D00` | 风险与待处理 |
| Destructive | `#FB7185` | `#C92A2A` | 删除与不可逆错误 |

约束：

- 品牌渐变仅允许在 Landing 的极小装饰或 Magic UI 边缘光束中使用暖橙到琥珀；按钮默认使用纯色。
- 每个页面和组件都必须在亮色、暗色模式下可读；禁止只为其中一种模式写死背景、前景或边框。
- 不使用蓝紫、紫粉或青紫渐变。
- 图表使用橙、琥珀、绿色、玫红与中性灰，保证色盲可辨；状态必须带文本。
- 非中性背景上的文字、边框和阴影要向同一暖色相轻微偏移。

### 2.3 字体

- UI 与正文：Geist Sans；中文按系统字体回退。
- 数字、时长、价格、模型 ID：Geist Mono，并使用等宽数字。
- 页面标题：`24–32 px / 1.2 / 650–700`。
- 区块标题：`16–18 px / 1.35 / 600`。
- 正文：`14 px / 1.6 / 400`；辅助文字不小于 `12 px`。
- Landing 主标题可以达到 `48–64 px`，但移动端从 `36 px` 起。
- 中文界面不使用英文式全大写标签；按钮和标题使用自然的句式表达。

### 2.4 间距、圆角与层级

- 基础间距：`4 px`；常用序列为 `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。
- 控件圆角：`8–10 px`；卡片：`12–16 px`；大容器最多 `20 px`。
- 子元素圆角不得大于父容器，嵌套时保持同心关系。
- 卡片使用清晰边框加双层柔和阴影；默认不使用大面积半透明玻璃。
- 内容页最大宽度按任务选择：表单 `768 px`、设置与编辑 `1024 px`、生产工作台 `1280 px`。

## 3. 页面类型

### 3.1 Landing / 工作台入口

- 目标：让首次用户在 10 秒内理解价值并开始上传、粘贴链接或输入主题。
- 使用暖橙品牌光、点阵/网格和一次轻量边缘光束建立识别度。
- 第一屏只突出一个主动作组；示例、最近项目和热点位于主动作之后。
- 免费与付费生成方式必须显示费用归属、预计耗时和产出差异。
- Landing 可以有独立视觉节奏，但颜色、字体、控件和交互状态仍服从本规范。

### 3.2 列表与资产页

- 标题、说明、主操作和筛选形成稳定页头。
- 卡片优先展示名称、状态、更新时间和下一动作，避免大片无信息留白。
- 空态包含解释、主要行动和可选示例；删除提供确认或可撤销窗口。

### 3.3 创建与配置页

- 按真实流程分段，步骤标题说明当前要完成的任务。
- 必填项带文字或符号并关联可访问标签；不预先禁用提交来隐藏验证反馈。
- 高级选项使用折叠区，不与默认路径争夺注意力。
- API Key 等敏感输入不进入日志、URL 或持久化设计文档。

### 3.4 项目生产页

- 页面级导航与项目步骤保持一致位置；当前阶段、完成阶段和异常阶段同时有图形与文本。
- 编辑区、预览区和检查区按任务关系布局，不机械套用相同卡片。
- 生成、分析和导出进度使用可中断或可恢复状态；异步更新通过 `aria-live="polite"` 宣布。

### 3.5 设置页

- 默认先展示推荐路径，再展示分平台高级配置。
- 平台卡片只在展开或选中时显示完整字段，降低首屏噪声。
- 每个设置说明即时保存、作用范围和测试状态。

## 4. 组件规范

### 4.1 基础组件

- 交互控件优先使用 `components/ui` 中的 shadcn/ui 组件；业务页面不重新实现 Button、Input、Select、Dialog、Tabs、Tooltip 等基础行为。
- 图标统一使用 Lucide，默认 `16 px`、`1.75–2 px` 描边；禁止手写近似 SVG 或 emoji 代替界面图标。
- `Button`：桌面高度至少 `36 px`，移动端主要操作至少 `44 px`；加载时保留原标签并显示进度。
- `Input`：移动端字号至少 `16 px`；占位符给出示例并以省略号 `…` 结尾。
- `Card`：默认有边框、表面色和克制阴影；只有可点击卡片才显示明显 hover。
- `Tabs`：用于同级内容切换，不代替跨页面导航；可分享状态优先进入 URL。
- `Dialog`：正确陷阱焦点，关闭后归还焦点；破坏性操作写清对象和后果。

### 4.2 Magic UI

- 只用于 Landing、首次连接成功、关键生成入口等高价值时刻。
- 推荐：`DotPattern` 作为低对比背景，`BorderBeam` 作为主卡片的一次暖色光束，`AnimatedShinyText` 用于非关键短标签。
- 禁止在设置、表格、长表单和每张卡片上重复动画。
- 所有动画支持 `prefers-reduced-motion`，不使用 `transition: all`。

## 5. 交互与状态

- 全流程可通过键盘完成，焦点环清晰且不被粘性区域遮挡。
- 桌面点击目标至少 `24 px`；移动端至少 `44 px`。
- 导航使用 `<Link>` / `<a>`，动作使用 `<button>`。
- 拖拽上传必须同时提供点击选择与键盘入口。
- 保存、生成、连接等异步状态使用“正在保存…”等自然文案，并避免短暂闪烁。
- 用户可安全预期成功时使用乐观更新；失败时回滚并提供重试或撤销。
- 可分享的标签、筛选、分页和展开状态优先写入 URL。
- 支持系统缩放，不禁用粘贴，不在输入过程中阻止用户键入。

## 6. 内容与本地化

- 默认中文文案直接、具体、行动导向；英文保持相同信息层级，不逐字直译。
- 一个概念只使用一个名称。例如统一使用“项目”“成片”“模型 Key”。
- 按钮说明结果，如“开始生成脚本”“保存 API Key”，避免“继续”“确认”。
- 使用数字表示数量，数字与单位之间留不换行空格，例如 `30 秒`、`10 MB`。
- 使用中文省略号 `…`，使用弯引号；标题避免孤行与尴尬断句。
- 日期、金额、时长和数字按当前语言环境格式化。

## 7. 可访问性与性能

- 页面拥有唯一、准确的标题和一个清晰的 `<h1>`；主内容提供跳转链接。
- 优先语义 HTML，再使用 ARIA；图标按钮必须有可描述的 `aria-label`。
- 装饰图案使用 `aria-hidden`，图片设置准确 `alt`，视频提供字幕或文本说明。
- 不仅依赖颜色表达状态；文字与背景维持可读对比。
- 所有动效遵循 `prefers-reduced-motion`；只动画 `opacity` 和 `transform` 等合成属性。
- 禁止 `transition: all`。
- 图片与视频预留明确尺寸；首屏必要资源优先加载，其余延迟加载。
- 大列表使用虚拟化或 `content-visibility`；昂贵媒体处理离开主线程。

## 8. 页面验收清单

每次设计或 UI 变更至少检查：

1. Landing：主动作在首屏可见，品牌风格独立且没有蓝紫渐变。
2. 项目列表、商品库、主播库：加载、空、少量、密集和删除状态完整。
3. 新建项目、热点成片、爆款复刻、批量任务：默认路径清晰，高级项按需出现。
4. 脚本、素材、生产、字幕、视频、导出：项目阶段与下一步一致，错误可恢复。
5. 设置：推荐路径、单平台配置、测试失败和敏感字段行为明确。
6. 桌面、移动与超宽布局；键盘导航、焦点、缩放和减少动画偏好。
7. 页面标题、空态文案、错误出口和异步播报。
8. 全仓搜索不得出现蓝紫品牌色、蓝紫渐变或无理由的硬编码品牌色。

## 9. 实现约束

- 设计令牌集中维护在 `src/app/globals.css`，组件通过语义变量消费。
- shadcn/ui 组件保留其无障碍行为，只通过变量、variants 与 `className` 做视觉适配。
- 业务组件不得复制基础控件实现，不新增与现有库重叠的依赖。
- 删除废弃样式与旧品牌色，不保留双主题 fallback。
- 设计改造完成后运行 lint、类型检查/构建、测试，并用浏览器检查关键页面。
