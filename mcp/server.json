{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ullrai/vibesku-clips",
  "description": "Drive VibeSKU Clips’ keyless short-video pipeline (script → free stock footage → free TTS → captions → BGM → FFmpeg compose) from any MCP client — one sentence or a product link becomes a finished vertical video.",
  "repository": {
    "url": "https://github.com/UllrAI/VibeSKU-Clips",
    "source": "github"
  },
  "version": "0.1.2",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "vibesku-clips-mcp",
      "version": "0.1.2",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "VIBESKU_CLIPS_BASE_URL",
          "description": "URL of the running VibeSKU Clips instance (default http://localhost:3000; start it with `pnpm dev` or `pnpm start`)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "VIBESKU_CLIPS_LLM_BASE_URL",
          "description": "OpenAI-compatible LLM endpoint for script generation (optional — footage and voiceover are keyless without it)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "VIBESKU_CLIPS_LLM_API_KEY",
          "description": "API key for the LLM endpoint (optional, only needed for script generation)",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "VIBESKU_CLIPS_LLM_MODEL",
          "description": "LLM model id for script generation (optional, e.g. deepseek-ai/deepseek-v3.2)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
